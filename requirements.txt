# Motor + backend. Deliberadamente corto: el pipeline usa sólo la biblioteca
# estándar, así que lo único obligatorio es el servidor HTTP y el Excel.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
openpyxl>=3.1

# Opcional — sólo para el modo de investigación con IA (ProveedorLLM).
# Sin esta dependencia el producto funciona igual en modo demo y web.
# anthropic>=0.40
